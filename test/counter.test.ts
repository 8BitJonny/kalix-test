/* This code was initialised by Kalix tooling.
 * As long as this file exists it will not be re-generated.
 * You are free to make changes to this file.
 */

import { MockValueEntity } from "@kalix-io/testkit";
import { expect } from "chai";
import counter from "../src/counter";

describe("ShoppingCart", () => {
  const entityId = "entityId";
  
  describe("UpCounter", () => {
    it("should...", async () => {
      const entity = new MockValueEntity(counter, entityId);
      // TODO: you may want to set fields in addition to the entity id
      // const result = await entity.handleCommand("UpCounter", { entityId });
      
      // expect(result).to.deep.equal({});
      // expect(entity.error).to.be.undefined;
      // expect(entity.state).to.deep.equal({});
    });
  });
  
  describe("DownCounter", () => {
    it("should...", async () => {
      const entity = new MockValueEntity(counter, entityId);
      // TODO: you may want to set fields in addition to the entity id
      // const result = await entity.handleCommand("DownCounter", { entityId });
      
      // expect(result).to.deep.equal({});
      // expect(entity.error).to.be.undefined;
      // expect(entity.state).to.deep.equal({});
    });
  });
  
  describe("HalfCounter", () => {
    it("should...", async () => {
      const entity = new MockValueEntity(counter, entityId);
      // TODO: you may want to set fields in addition to the entity id
      // const result = await entity.handleCommand("HalfCounter", { entityId });
      
      // expect(result).to.deep.equal({});
      // expect(entity.error).to.be.undefined;
      // expect(entity.state).to.deep.equal({});
    });
  });
  
  describe("ResetCounter", () => {
    it("should...", async () => {
      const entity = new MockValueEntity(counter, entityId);
      // TODO: you may want to set fields in addition to the entity id
      // const result = await entity.handleCommand("ResetCounter", { entityId });
      
      // expect(result).to.deep.equal({});
      // expect(entity.error).to.be.undefined;
      // expect(entity.state).to.deep.equal({});
    });
  });
  
  describe("GetCounter", () => {
    it("should...", async () => {
      const entity = new MockValueEntity(counter, entityId);
      // TODO: you may want to set fields in addition to the entity id
      // const result = await entity.handleCommand("GetCounter", { entityId });
      
      // expect(result).to.deep.equal({});
      // expect(entity.error).to.be.undefined;
      // expect(entity.state).to.deep.equal({});
    });
  });
  
  describe("SetCounter", () => {
    it("should...", async () => {
      const entity = new MockValueEntity(counter, entityId);
      // TODO: you may want to set fields in addition to the entity id
      // const result = await entity.handleCommand("SetCounter", { entityId });
      
      // expect(result).to.deep.equal({});
      // expect(entity.error).to.be.undefined;
      // expect(entity.state).to.deep.equal({});
    });
  });
});